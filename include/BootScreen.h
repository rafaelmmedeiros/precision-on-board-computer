#pragma once

// Static splash shown during the natural boot window (WiFi + NTP).
// No status text, no progress bar — boot completes and the System screen
// takes over on the first loop iteration.
void displayBoot();
